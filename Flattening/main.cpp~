#include <bits/stdc++.h>

using namespace std;

int main() {
    vector<int> outputs;
    int t, n, k;
    scanf("%d", t);
    for(int i = 0; i < t; i++) {
        scanf("%d %d",n,k);
        vector<int> values;
        int save = 0, temp;
        for(int j = 0; j < n; j++) {
            scanf("%d",temp);
            if(temp != save) {
                values.push_back(temp);
                save = temp;
            } 
        }
        
    for(int i = 0; i < values.size(); i++) {
        cout << values[i] << " ";
    }   
    cout << endl;    
    }
    
}
